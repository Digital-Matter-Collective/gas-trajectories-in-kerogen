---
name: Bug report
about: Report incorrect behavior, a crash, or a reproduction mismatch
title: ""
labels: bug
assignees: ""
---

## Description

A clear description of what went wrong.

## Steps to reproduce

The exact command(s) run, including flags. If this is a reproduction
mismatch (output differs from a paper figure/table), name the figure/table
and the section of `docs/reproduction.md` you followed.

```bash

```

## Expected behavior

## Actual behavior

Include the full error traceback if there is one.

## Environment

- OS:
- Python version:
- Install method: `conda env create -f environment.yml` / `pip install` / other
- Output of `pip list` or `conda list` for the relevant packages (numpy, scipy,
  numba, vtk), if the issue might be version-related:

## Additional context

Anything else that might help — input data shape/size, whether this
reproduces with `--seed` fixed, etc.
